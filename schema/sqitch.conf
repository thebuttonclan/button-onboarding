[core]
	engine = pg
	# plan_file = sqitch.plan
	# top_dir = .
# [engine "pg"]
	# target = db:pg:
	# registry = sqitch
	# client = psql
[target "onboarding_db"]
	uri = db:pg:onboarding
[deploy]
	verify = true
[rebase]
	verify = true
